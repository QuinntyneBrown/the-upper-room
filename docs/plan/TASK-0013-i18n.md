---
id: TASK-0013
title: i18n with Transloco (en-CA)
status: Draft
phase: F
depends_on: [TASK-0001]
traces_to: [L2-100, L2-110]
estimated_context: small
---

# TASK-0013: i18n setup

## Goal
Wire `@jsverse/transloco` with `en-CA` as the default locale, lazy-loaded JSON files per feature scope, and a `keys-only` rule enforced via TASK-0006's `i18n-no-literal` lint.

## ATDD Process — REQUIRED

## Acceptance Tests

### Playwright E2E

**Spec file:** `frontend/projects/the-upper-room/e2e/tests/foundation/i18n.spec.ts`

**Page Object:** `pages/StyleguidePage.greeting()`.

**Scenarios:**
1. The styleguide greeting reads "Hello" sourced from `en-CA.json` key `styleguide.greeting`.
2. Switching the active language to a fake locale `xx-XX` (registered in test) renders the fake translation, proving keys are not hard-coded.
3. Missing keys fall back to the key string and emit a warning in dev only.

### Unit (frontend)
- `i18n-no-literal` lint test ensures the existing codebase passes.

## Implementation Outline
- `projects/the-upper-room/src/assets/i18n/en-CA.json`, `en-CA.styleguide.json`, etc.
- Date/number formatting via Transloco-locale plugin (full date pipe rules in TASK-0170).

## Definition of Done
- [ ] All literal user-facing strings replaced with keys.
