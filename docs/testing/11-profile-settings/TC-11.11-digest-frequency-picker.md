---
id: TASK-TC-11.11
title: 'Run TC-11.11 - Digest frequency picker'
status: Completed
test_id: TC-11.11
source: ../../test-plan/11-profile-settings.md
result: PASS
run_at: 2026-05-10T02:30:00Z
---

# TASK-TC-11.11: Run TC-11.11 - Digest frequency picker

## Result: PASS — backend GET/PUT + frontend select shipped

| Field      | Value                       |
|------------|-----------------------------|
| Backend    | xUnit (.NET)                |
| Browser    | Chromium (Playwright)       |
| Build SHA  | 2758fae + digest stack      |
| Run at     | 2026-05-10T02:30:00Z        |

### Evidence

Backend ATDD (new this iteration):
- `Digest_default_is_off_then_put_persists_across_restart` PASS — initial GET returns
  `{digestFrequency: "off"}`, PUT `{digestFrequency: "weekly"}` persists across factory restart.
- `Digest_invalid_frequency_returns_422` PASS — values outside `off|daily|weekly|monthly` rejected.
- `DigestPreferenceRow` added to `NotificationsDbContext` (one row per user).

Frontend:
- `tar-notification-preferences.html` now renders an "Email digest frequency" `<select>` with
  `data-testid="digest-frequency-select"` and four options (off, daily, weekly, monthly).
- `onDigestChange()` PUTs the new value and shows a `digest-saved` indicator briefly.

End-to-end digest preference now persists. [BUG-011](../../bugs/BUG-011-digest-frequency-missing.md)
is resolved.

## Definition of Done

- [x] Test run completed.
- [x] Result recorded.
- [x] Defect linked for any failure.
