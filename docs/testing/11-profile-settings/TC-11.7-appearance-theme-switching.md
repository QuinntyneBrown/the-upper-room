---
id: TASK-TC-11.7
title: 'Run TC-11.7 - Appearance: theme switching'
status: Completed
test_id: TC-11.7
source: ../../test-plan/11-profile-settings.md
result: PASS
run_at: 2026-05-10T00:21:00Z
---

# TASK-TC-11.7: Run TC-11.7 - Appearance: theme switching

## Result: PASS (with note: now uses mat-button-toggle-group not native radiogroup)

| Field      | Value                       |
|------------|-----------------------------|
| Browser    | Chromium (Playwright)       |
| Viewport   | 1280×720                    |
| Build SHA  | 235ebfc                     |
| Run at     | 2026-05-10T00:21:00Z        |

### Evidence

`appearance.html` and `appearance.ts:12` (`modes: ThemeMode[] = ['system', 'light', 'dark']`):

- Heading "Appearance".
- `mat-button-toggle-group` with `aria-label="Theme mode"` (Material's group acts as a radio
  group with single-select semantics; this is a deliberate Material upgrade from the prior
  `role="radiogroup"` div).
- Three options in order **system | light | dark**, each with `[attr.data-testid]="'theme-option-' + m"`.
- `(change)="theme.setMode($event.value)"` updates the ThemeService which writes
  `<html data-theme="...">` and persists to `localStorage.theme`.

Behaviour is correct; only the markup style has shifted from a hand-rolled radiogroup to a
Material toggle group. Test plan literal "div role='radiogroup'" assertion is now technically
stale but the user-facing semantics and testids are preserved.

## Definition of Done

- [x] Test run completed.
- [x] Result recorded.
- [x] Defect linked for any failure.
