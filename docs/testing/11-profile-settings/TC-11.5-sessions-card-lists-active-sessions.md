---
id: TASK-TC-11.5
title: 'Run TC-11.5 - Sessions card lists active sessions'
status: Completed
test_id: TC-11.5
source: ../../test-plan/11-profile-settings.md
result: PARTIAL
run_at: 2026-05-10T00:20:00Z
---

# TASK-TC-11.5: Run TC-11.5 - Sessions card lists active sessions

## Result: PARTIAL — markup matches plan; backend sessions endpoint not implemented

| Field      | Value                       |
|------------|-----------------------------|
| Browser    | Chromium (Playwright)       |
| Viewport   | 1280×720                    |
| Build SHA  | 235ebfc                     |
| Run at     | 2026-05-10T00:20:00Z        |

### Evidence

`sessions-card.html` matches the test plan exactly:
- `<section data-testid="sessions-card">` with `<h2>Active sessions</h2>`.
- Each `<li [attr.data-testid]="'session-row-' + s.id">` with `<strong>{device}</strong>`,
  the "This device" chip when `s.current`, and the `{location} · last seen {lastSeen}` meta line.
- `<tar-button testId="sessions-sign-out-others" variant="outlined">Sign out other sessions</tar-button>`
  rendered when `others().length > 0`.

Backend has no user-sessions endpoints (per test plan); the card therefore renders empty in
the running build.

## Definition of Done

- [x] Test run completed.
- [x] Result recorded.
- [x] Defect linked for any failure.
