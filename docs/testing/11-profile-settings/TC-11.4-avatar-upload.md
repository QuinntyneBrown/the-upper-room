---
id: TASK-TC-11.4
title: 'Run TC-11.4 - Avatar upload'
status: Completed
test_id: TC-11.4
source: ../../test-plan/11-profile-settings.md
result: PARTIAL
run_at: 2026-05-10T00:20:00Z
---

# TASK-TC-11.4: Run TC-11.4 - Avatar upload

## Result: PARTIAL — UI affordance present; persistence depends on profile endpoint (TC-11.3)

| Field      | Value                       |
|------------|-----------------------------|
| Browser    | Chromium (Playwright)       |
| Viewport   | 1280×720                    |
| Build SHA  | 235ebfc                     |
| Run at     | 2026-05-10T00:20:00Z        |

### Evidence

`<tar-avatar-uploader>` from the components library is wired in `my-profile.html:4` and emits
`(fileSelected)`. The handler `onAvatarSelected($event)` posts to `/api/v1/uploads` with a
multipart form field named `file` and updates the form's `avatarUrl` optimistically. On error,
the snackbar surfaces the catalog message.

End-to-end persistence across reload is gated on the profile read endpoint that returns
`avatarUrl` — currently incomplete (see TC-11.3). Local optimistic update works.

## Definition of Done

- [x] Test run completed.
- [x] Result recorded.
- [x] Defect linked for any failure.
