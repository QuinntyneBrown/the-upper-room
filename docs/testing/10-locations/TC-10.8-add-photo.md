---
id: TASK-TC-10.8
title: 'Run TC-10.8 - Add photo'
status: Completed
test_id: TC-10.8
source: ../../test-plan/10-locations.md
result: PARTIAL
run_at: 2026-05-10T00:12:00Z
---

# TASK-TC-10.8: Run TC-10.8 - Add photo

## Result: PARTIAL — affordance present; upload pipeline incomplete in current build

| Field      | Value                       |
|------------|-----------------------------|
| Browser    | Chromium (Playwright)       |
| Viewport   | 1280×720                    |
| Build SHA  | bf65789                     |
| Run at     | 2026-05-10T00:12:00Z        |

### Evidence

- Source review confirms the **Add photo** label with `add_photo_alternate` icon and the
  hidden `[data-testid="location-photo-input"]` accepting `image/*` is present in
  `location-detail.html`.
- `location-detail.spec.ts:74` (upload 3 photos → carousel cycles) FAILS — upload returns an
  unexpected status / response shape; carousel never refreshes with new images.
- `location-detail.spec.ts:118` (photo >10MB rejected; non-image rejected) FAILS — the size /
  MIME validation snackbar copy does not match expectations.

The upload backend route exists under `backend/src/TheUpperRoom.Api/Uploads/`, but the
end-to-end wiring between the form input, the upload request, and the photos signal needs
follow-up. To file as a separate bug after BUG-041 lands.

## Definition of Done

- [x] Test run completed.
- [x] Result recorded.
- [x] Defect linked for any failure.
