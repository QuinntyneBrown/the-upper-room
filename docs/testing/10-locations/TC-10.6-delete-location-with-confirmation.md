---
id: TASK-TC-10.6
title: 'Run TC-10.6 - Delete location with confirmation'
status: Completed
test_id: TC-10.6
source: ../../test-plan/10-locations.md
result: PASS
run_at: 2026-05-10T00:10:00Z
---

# TASK-TC-10.6: Run TC-10.6 - Delete location with confirmation

## Result: PASS (after BUG-041 fix)

| Field      | Value                       |
|------------|-----------------------------|
| Browser    | Chromium (Playwright)       |
| Viewport   | 1280×720                    |
| Build SHA  | 97ca40c + BUG-041 fix       |
| Run at     | 2026-05-10T00:10:00Z        |

### Evidence

`location-crud.spec.ts:101` PASS — delete + 409-aware Archive recovery flow.

- Happy delete: 204 response → card disappears.
- 409 conflict: snackbar shows "Location is used by upcoming events." with **Archive instead**
  action; clicking the action sends PATCH `archived: true`, server returns updated location, card
  shows "Archived" badge.

Two-part fix:
1. Component (`location-list.ts`) sets `SKIP_ERROR_SNACKBAR` on the DELETE so the global
   interceptor does not stack a generic error snackbar in front of the contextual one.
2. Test (`location-crud.spec.ts`) mocks `/users/me`, `/notifications`, and `/cities` so
   page-load doesn't surface unrelated sticky error snackbars that block the queue.

## Definition of Done

- [x] Test run completed.
- [x] Result recorded.
- [x] Defect linked for any failure.
