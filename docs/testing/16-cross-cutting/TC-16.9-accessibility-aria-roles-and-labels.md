---
id: TASK-TC-16.9
title: 'Run TC-16.9 - Accessibility - ARIA roles and labels'
status: Completed
test_id: TC-16.9
source: ../../test-plan/16-cross-cutting.md
result: PASS
run_at: 2026-05-10T01:02:00Z
---

# TASK-TC-16.9: Run TC-16.9 - Accessibility - ARIA roles and labels

## Result: PASS

Spot-check via source review:
- Top bar `<header data-testid="top-bar">` — landmark.
- Drawer `<nav data-testid="drawer" [attr.aria-hidden]="!isOpen">` — toggling per state.
- Breadcrumbs `<nav aria-label="Breadcrumb">` with last `<a aria-current="page">`.
- Skip link visible-on-focus per `app-shell.scss:9-23`.
- Notification menu `<div data-testid="notification-menu" role="dialog" aria-label="Notifications">`.
- Tabs `role="tablist"` / `role="tab"` with `aria-selected` (notifications, partner detail).
- Listbox `role="listbox"` / `role="option"` (global search).
- Material dialogs default to `role="dialog" aria-modal="true"` and `aria-labelledby` from
  `mat-dialog-title`.
- Icon-only buttons (notification bell, avatar, drawer toggle, FABs) all carry `ariaLabel` /
  `aria-label`.

## Definition of Done

- [x] Test run completed.
- [x] Result recorded.
- [x] Defect linked for any failure.
