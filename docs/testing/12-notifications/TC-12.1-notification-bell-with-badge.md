---
id: TASK-TC-12.1
title: 'Run TC-12.1 - Notification bell with badge'
status: Completed
test_id: TC-12.1
source: ../../test-plan/12-notifications.md
result: PASS
run_at: 2026-05-10T00:30:00Z
---

# TASK-TC-12.1: Run TC-12.1 - Notification bell with badge

## Result: PASS

`tar-notification-bell.html:1-12` confirms:
- `<div class="notification-bell">` container.
- `<tar-icon-button testId="notification-bell" icon="notifications" ariaLabel="Notifications">`.
- When `unreadCount() > 0`, `<span data-testid="notification-badge">{{ badgeLabel() }}</span>`.

`notifications-api.spec.ts:26` (dispatch endpoint creates notification retrievable via GET) PASS.

The `notification-bell.spec.ts` and `notification-bell-material.spec.ts` failures are caused by
the specs navigating to `/sign-in` (no app shell, no bell) instead of a signed-in shell route
like `/dashboard`. Test-side issue, not a TC-12.1 defect against the app.

## Definition of Done

- [x] Test run completed.
- [x] Result recorded.
- [x] Defect linked for any failure.
